import { FormEvent, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import Brand from "@/components/Brand";
import { loginManager } from "@/lib/auth";
import { backendEnabled, backendLogin, getBackendEmployees } from "@/lib/backend";
import { hydrateLocalData } from "@/lib/cloudDataSync";
import { setManagerSession, saveEmployees } from "@/lib/storage";
export default function ManagerLogin(){const navigate=useNavigate();const[username,setUsername]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState<string|null>(null);const[loading,setLoading]=useState(false);const submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();if(loading)return;setError(null);setLoading(true);try{if(backendEnabled){const user=await backendLogin(username,password);setManagerSession({loginAt:new Date().toISOString(),name:user.name,role:user.role,jobNumber:user.username,accountId:user.id});try{const employees=await getBackendEmployees();saveEmployees(employees);await hydrateLocalData()}catch{}navigate("/manager",{replace:true});return}const result=loginManager(password,username);if(!result.success){setError(result.reason||"اسم المستخدم أو كلمة المرور غير صحيحة");return}navigate("/manager",{replace:true})}catch(e){setError(e instanceof Error?e.message:"تعذر الاتصال بالخادم")}finally{setLoading(false)}};return <div className="min-h-screen flex flex-col"><header className="p-5"><Brand /></header><main className="flex-1 grid place-items-center px-5 pb-10"><div className="w-full max-w-md hud-card p-7"><div className="text-xs mono text-muted-foreground">MANAGER · لوحة التحكم</div><h1 className="text-2xl font-extrabold mt-1">دخول الإدارة</h1><p className="text-sm text-muted-foreground mt-1">الحسابات الإدارية التي تم تفعيلها فقط يمكنها الدخول إلى لوحة التحكم.</p><form onSubmit={submit} className="mt-6 space-y-4"><div><label className="block text-sm font-semibold mb-1.5" htmlFor="manager-username">اسم المستخدم</label><input id="manager-username" type="text" autoComplete="username" className="input w-full" value={username} onChange={e=>setUsername(e.target.value)} placeholder="أدخل اسم المستخدم" required /></div><div><label className="block text-sm font-semibold mb-1.5" htmlFor="manager-password">كلمة المرور</label><input id="manager-password" type="password" autoComplete="current-password" className="input w-full" value={password} onChange={e=>setPassword(e.target.value)} placeholder="أدخل كلمة المرور" required /></div>{error&&<div className="rounded-xl border border-destructive/40 bg-destructive/10 text-destructive-foreground p-3 text-sm" role="alert">{error}</div>}<button type="submit" className="btn-primary w-full py-3" disabled={loading}>{loading?"جاري التحقق...":"دخول الإدارة"}</button></form><div className="mt-5 text-xs text-center flex justify-between items-center gap-4"><Link to="/" className="text-muted-foreground hover:text-foreground">← العودة للرئيسية</Link><Link to="/login" className="text-primary hover:underline">دخول الموظفين</Link></div></div></main></div>}
