import { readFileSync, writeFileSync } from "node:fs";

const file = new URL("../src/pages/ManagerReports.tsx", import.meta.url);
let source = readFileSync(file, "utf8");

if (source.includes("const refreshDailyReportSnapshot = async ()")) {
  console.log("ManagerReports live patch: already applied.");
  process.exit(0);
}

const settingsAnchor = '  const settings = getSettings();\n';
const snapshot = `  const refreshDailyReportSnapshot = async () => {\n    setLoading(true);\n    setError(null);\n    try {\n      const [freshEmployeesRaw, freshAuditRaw, freshRequestsRaw, freshDailyRaw] = await Promise.all([\n        getBackendEmployees(),\n        getBackendAudit(2000),\n        getBackendRequests("admin"),\n        getDailyStatus(date),\n      ]);\n      if (!Array.isArray(freshEmployeesRaw)) throw new Error("تعذر تحديث قائمة الموظفين من D1");\n      if (!Array.isArray(freshAuditRaw)) throw new Error("تعذر تحديث سجلات الحضور من D1");\n      if (!Array.isArray(freshRequestsRaw)) throw new Error("تعذر تحديث الطلبات من D1");\n      if (!freshDailyRaw || !Array.isArray(freshDailyRaw.employees)) throw new Error("تعذر إعادة احتساب الحالة اليومية من D1");\n      const freshEmployees = freshEmployeesRaw as Employee[];\n      const freshAudit = freshAuditRaw as Audit[];\n      const freshRequests = freshRequestsRaw as RequestRow[];\n      const freshDailyStatus = freshDailyRaw.employees as DailyStatusRow[];\n      const freshIndex = auditIndex(freshAudit);\n      const freshDailyStatusMap = new Map(freshDailyStatus.map(row => [row.employeeId, row]));\n      const freshCalculated = freshEmployees.map(employee => {\n        const employeeDates = datesForEmployee(employee, dates);\n        return calculateSummary(employee, employeeDates, freshIndex, settings, freshRequests, freshDailyStatusMap);\n      });\n      const freshSummaries = freshCalculated.filter(s => s.workDays > 0);\n      setEmployees(freshEmployees);\n      setAudit(freshAudit);\n      setRequests(freshRequests);\n      setDailyStatus(freshDailyStatus);\n      setLoading(false);\n      return { summaries: freshSummaries, index: freshIndex, requests: freshRequests, dailyStatusMap: freshDailyStatusMap };\n    } catch (cause) {\n      setLoading(false);\n      const message = cause instanceof Error ? cause.message : "فشل تحديث بيانات التقرير";\n      setError(message);\n      return null;\n    }\n  };\n`;
if (!source.includes(settingsAnchor)) throw new Error("ManagerReports: settings anchor not found.");
source = source.replace(settingsAnchor, settingsAnchor + snapshot);

const oldCsv = /  const exportCsv = \(\) => \{[\s\S]*?\n  const exportExcel = \(\) => \{/;
const newCsv = `  const exportCsv = async () => {\n    let reportSummaries = summaries;\n    let reportIndex = index;\n    let reportRequests = requests;\n    let reportDailyStatusMap = dailyStatusMap;\n    if (mode === "daily") {\n      const fresh = await refreshDailyReportSnapshot();\n      if (!fresh) return;\n      reportSummaries = fresh.summaries;\n      reportIndex = fresh.index;\n      reportRequests = fresh.requests;\n      reportDailyStatusMap = fresh.dailyStatusMap;\n    }\n    const h = ["اسم الشركة", "الاختصاص", "الموظف", "الرقم الوظيفي", "التاريخ", "اليوم", "الحالة", "وقت الحضور", "وقت الانصراف", "مدة العمل", "دقائق التأخر", "دقائق الانصراف المبكر", "تفصيل اليوم"], d = reportSummaries.flatMap(s => calculateDetails(s.employee, datesForEmployee(s.employee, dates), reportIndex, settings, reportRequests, mode === "daily" ? reportDailyStatusMap : undefined).map(day => [String(settings.brandName || "HADIR").trim() || "HADIR", specialtyOf(s.employee), s.employee.name, s.employee.jobNumber, day.date, day.day, labels[day.status], day.checkIn, day.checkOut, formatDurationMinutes(day.worked), day.late, day.early, day.detail] as CsvCell[]));\n    downloadCSV("Hadir-" + mode + "-" + period + "-attendance", h, d);\n  };\n  const exportExcel = async () => {`;
if (!oldCsv.test(source)) throw new Error("ManagerReports: export anchors not found.");
source = source.replace(oldCsv, newCsv);

const oldExcel = /  const exportExcel = async \(\) => \{[\s\S]*?\n  const title = /;
const newExcel = `  const exportExcel = async () => {\n    let reportSummaries = summaries;\n    let reportIndex = index;\n    let reportRequests = requests;\n    let reportDailyStatusMap = dailyStatusMap;\n    if (mode === "daily") {\n      const fresh = await refreshDailyReportSnapshot();\n      if (!fresh) return;\n      reportSummaries = fresh.summaries;\n      reportIndex = fresh.index;\n      reportRequests = fresh.requests;\n      reportDailyStatusMap = fresh.dailyStatusMap;\n    }\n    const sourceRows = reportSummaries.map(s => ({ employee: s.employee, specialty: specialtyOf(s.employee) }));\n    const dailyRows = sourceRows.flatMap(row => { const employee = row.employee, details = calculateDetails(employee, datesForEmployee(employee, dates), reportIndex, settings, reportRequests, mode === "daily" ? reportDailyStatusMap : undefined); return details.map(day => ({ employee: employee.name, jobNumber: employee.jobNumber, specialty: specialtyOf(employee), date: day.date, day: day.day, status: labels[day.status], checkIn: day.checkIn, checkOut: day.checkOut, worked: formatDurationMinutes(day.worked), late: day.late, early: day.early, detail: day.detail })); });\n    const absenceRows = dailyRows.filter(row => row.status === "غياب");\n    const reportTotal = reportSummaries.reduce((a, s) => ({ present: a.present + s.present, absent: a.absent + s.absent, early: a.early + s.early, late: a.late + s.late, open: a.open + s.open, permission: a.permission + s.permission, leave: a.leave + s.leave, off: a.off + s.off }), { present: 0, absent: 0, early: 0, late: 0, open: 0, permission: 0, leave: 0, off: 0 });\n    const reportChartData = [{ label: "حاضر", value: reportTotal.present }, { label: "غياب", value: reportTotal.absent }, { label: "استئذان", value: reportTotal.permission }, { label: "إجازة", value: reportTotal.leave }, { label: "انصراف مبكر", value: reportTotal.early }, { label: "تأخر", value: reportTotal.late }, { label: "تسجيل ناقص", value: reportTotal.open }];\n    downloadProfessionalAttendanceReport({ mode, period, generatedAt: new Date().toLocaleString("ar-EG"), summaries: reportSummaries, dailyRows, absenceRows, chartData: reportChartData });\n  };\n  const title = `;
if (!oldExcel.test(source)) throw new Error("ManagerReports: Excel export anchor not found.");
source = source.replace(oldExcel, newExcel);

const oldPrint = '  const printReport = () => window.print();';
const newPrint = '  const printReport = async () => { if (mode !== "daily") { window.print(); return; } const fresh = await refreshDailyReportSnapshot(); if (!fresh) return; window.requestAnimationFrame(() => window.print()); };';
if (!source.includes(oldPrint)) throw new Error("ManagerReports: print anchor not found.");
source = source.replace(oldPrint, newPrint);

writeFileSync(file, source, "utf8");
console.log("ManagerReports live patch: daily reports now refresh D1 snapshot at generation time.");
