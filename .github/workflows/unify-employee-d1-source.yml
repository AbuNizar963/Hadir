name: Unify employee D1 source

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: echo "Legacy source patcher retired. Employee D1 synchronization is maintained by the main CI workflow."
