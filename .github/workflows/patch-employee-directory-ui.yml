name: One-time employee directory parity patch

on:
  push:
    branches: [Apps]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: Apps
          fetch-depth: 1
      - name: Patch employee directory UI
        env:
          TARGET: lib/pages/admin_management_page.dart
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          path = Path('lib/pages/admin_management_page.dart')
          source = path.read_text(encoding='utf-8')

          old_helpers = """  Widget _metric(String label, String value, IconData icon) => Expanded(child: Card(child: Padding(padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 13), child: Column(children: [Icon(icon, color: HadirBrand.primary), const SizedBox(height: 6), Text(value, style: const TextStyle(fontSize: 20, fontWeight: FontWeight.w900)), Text(label, textAlign: TextAlign.center, style: const TextStyle(fontSize: 10))]))));
  Widget _searchBox({required String hint, required ValueChanged<String> onChanged}) => TextField(onChanged: onChanged, decoration: InputDecoration(prefixIcon: const Icon(Icons.search_rounded), hintText: hint, isDense: true));
  Map<String, dynamic> _employeeWithControls(Map<String, dynamic> employee) { final id = '${employee['id'] ?? ''}'; final controls = _workforceControls[id]; return controls == null ? employee : {...employee, ...controls}; }
  Widget _infoPill(String label, String value) => Container(padding: const EdgeInsets.symmetric(horizontal: 9, vertical: 7), decoration: BoxDecoration(color: Theme.of(context).colorScheme.surfaceContainerHighest.withValues(alpha: .45), borderRadius: BorderRadius.circular(10)), child: Text('$label: $value', style: const TextStyle(fontSize: 10, fontWeight: FontWeight.w700)));
  Widget _smallAction(String label, IconData icon, VoidCallback onTap) => Padding(padding: const EdgeInsets.only(left: 6), child: OutlinedButton.icon(onPressed: onTap, icon: Icon(icon, size: 15), label: Text(label), style: OutlinedButton.styleFrom(padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 9), textStyle: const TextStyle(fontSize: 10, fontWeight: FontWeight.w800))));
  Widget _filterChip(String label, String value, String current, ValueChanged<String> onSelected) => ChoiceChip(label: Text(label), selected: current == value, onSelected: (_) => onSelected(value));
"""
          new_helpers = """  Widget _metric(String label, String value, IconData icon) => Expanded(child: Card(child: Padding(padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 13), child: Column(children: [Icon(icon, color: HadirBrand.primary), const SizedBox(height: 6), Text(value, style: const TextStyle(fontSize: 20, fontWeight: FontWeight.w900)), Text(label, textAlign: TextAlign.center, style: const TextStyle(fontSize: 10))]))));
  Widget _directoryStat(String label, String value, Color color) => Container(padding: const EdgeInsets.all(14), decoration: BoxDecoration(color: color.withValues(alpha: .07), borderRadius: BorderRadius.circular(16), border: Border.all(color: color.withValues(alpha: .25))), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [Row(mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [Text(label, style: TextStyle(color: color, fontSize: 11, fontWeight: FontWeight.w800)), Container(width: 7, height: 7, decoration: BoxDecoration(color: color, shape: BoxShape.circle))]), const SizedBox(height: 8), Text(value, style: const TextStyle(fontSize: 24, fontWeight: FontWeight.w900))]));
  Widget _searchBox({required String hint, required ValueChanged<String> onChanged}) => TextField(onChanged: onChanged, decoration: InputDecoration(prefixIcon: const Icon(Icons.search_rounded), hintText: hint, isDense: true));
  Map<String, dynamic> _employeeWithControls(Map<String, dynamic> employee) { final id = '${employee['id'] ?? ''}'; final controls = _workforceControls[id]; return controls == null ? employee : {...employee, ...controls}; }
  Widget _infoPill(String label, String value) => Container(padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 8), decoration: BoxDecoration(color: Theme.of(context).colorScheme.surfaceContainerHighest.withValues(alpha: .45), borderRadius: BorderRadius.circular(11), border: Border.all(color: Theme.of(context).colorScheme.outlineVariant.withValues(alpha: .45))), child: Text('$label: $value', style: const TextStyle(fontSize: 10, fontWeight: FontWeight.w700)));
  Widget _smallAction(String label, IconData icon, VoidCallback onTap, {Color? color}) => SizedBox(width: 64, child: OutlinedButton(onPressed: onTap, style: OutlinedButton.styleFrom(padding: const EdgeInsets.symmetric(horizontal: 4, vertical: 7), minimumSize: const Size(0, 50), side: BorderSide(color: (color ?? Theme.of(context).colorScheme.outline).withValues(alpha: .45)), shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))), child: Column(mainAxisSize: MainAxisSize.min, children: [Icon(icon, size: 17, color: color), const SizedBox(height: 3), Text(label, textAlign: TextAlign.center, style: TextStyle(fontSize: 9, fontWeight: FontWeight.w800, color: color))]));
  Widget _filterChip(String label, String value, String current, ValueChanged<String> onSelected) => ChoiceChip(label: Text(label), selected: current == value, onSelected: (_) => onSelected(value));
"""
          if old_helpers not in source:
              raise SystemExit('expected helper block not found')
          source = source.replace(old_helpers, new_helpers, 1)

          new_view = r'''  Widget _employeesView() {
    final filtered = _employees.where((raw) {
      if (raw is! Map) return false;
      final employee = _employeeWithControls(Map<String, dynamic>.from(raw));
      final query = _employeeQuery.trim().toLowerCase();
      final name = '${employee['name'] ?? ''}'.toLowerCase();
      final job = '${employee['jobNumber'] ?? ''}'.toLowerCase();
      final device = '${employee['deviceLabel'] ?? employee['deviceId'] ?? ''}'.toLowerCase();
      final status = '${employee['status'] ?? 'active'}'.toLowerCase();
      final schedule = '${employee['scheduleType'] ?? 'ADMIN'}';
      return (query.isEmpty || name.contains(query) || job.contains(query) || device.contains(query)) &&
          (_employeeStatus == 'all' || status == _employeeStatus) &&
          (_employeeSchedule == 'all' || schedule == _employeeSchedule);
    }).toList();

    final active = _employees.where((r) => r is Map && '${r['status'] ?? 'active'}' == 'active').length;
    final suspended = _employees.length - active;
    final devices = _employees.where((r) => r is Map && '${r['deviceId'] ?? ''}'.trim().isNotEmpty).length;
    final vip = _employees.where((r) => r is Map && _employeeWithControls(Map<String, dynamic>.from(r))['isVip'] == true).length;
    final escaped = _employees.where((r) => r is Map && '${r['escapeStatus'] ?? r['fieldStatus'] ?? ''}'.toLowerCase() == 'escaped').length;

    return Column(crossAxisAlignment: CrossAxisAlignment.stretch, children: [
      Container(
        padding: const EdgeInsets.all(18),
        decoration: BoxDecoration(
          color: Theme.of(context).colorScheme.primary.withValues(alpha: .025),
          borderRadius: BorderRadius.circular(20),
          border: Border.all(color: Theme.of(context).colorScheme.primary.withValues(alpha: .25)),
        ),
        child: Row(crossAxisAlignment: CrossAxisAlignment.center, children: [
          Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
            const Text('EMPLOYEE DIRECTORY', style: TextStyle(fontSize: 11, fontWeight: FontWeight.w800, letterSpacing: .4, color: HadirBrand.primary)),
            const SizedBox(height: 4),
            const Text('إدارة الموظفين', style: TextStyle(fontSize: 24, fontWeight: FontWeight.w900)),
            const SizedBox(height: 4),
            Text('الواجهة ثابتة أثناء المزامنة؛ يتم تحديث القيم المتغيرة فقط.', style: TextStyle(fontSize: 11, height: 1.5, color: Theme.of(context).colorScheme.onSurfaceVariant)),
          ])),
          const SizedBox(width: 12),
          FilledButton.icon(onPressed: _addEmployee, icon: const Icon(Icons.person_add_alt_1, size: 18), label: const Text('+ إضافة موظف')),
        ]),
      ),
      const SizedBox(height: 12),
      Container(
        padding: const EdgeInsets.all(12),
        decoration: BoxDecoration(color: Theme.of(context).colorScheme.primary.withValues(alpha: .025), borderRadius: BorderRadius.circular(20), border: Border.all(color: Theme.of(context).colorScheme.primary.withValues(alpha: .18))),
        child: LayoutBuilder(builder: (context, constraints) {
          final count = constraints.maxWidth >= 900 ? 6 : constraints.maxWidth >= 600 ? 3 : 2;
          return GridView.count(crossAxisCount: count, shrinkWrap: true, physics: const NeverScrollableScrollPhysics(), crossAxisSpacing: 8, mainAxisSpacing: 8, childAspectRatio: 1.45, children: [
            _directoryStat('الإجمالي', '${_employees.length}', const Color(0xFF38A9E8)),
            _directoryStat('فعال', '$active', const Color(0xFF16A34A)),
            _directoryStat('موقوف', '$suspended', const Color(0xFFE11D48)),
            _directoryStat('أجهزة موثقة', '$devices', const Color(0xFF8B5CF6)),
            _directoryStat('VIP مفعل', '$vip', const Color(0xFFF59E0B)),
            _directoryStat('هارب الآن', '$escaped', const Color(0xFFEF4444)),
          ];
        }),
      ),
      const SizedBox(height: 12),
      Container(
        padding: const EdgeInsets.all(18),
        decoration: BoxDecoration(color: Theme.of(context).colorScheme.surface.withValues(alpha: .78), borderRadius: BorderRadius.circular(20), border: Border.all(color: Theme.of(context).colorScheme.outlineVariant.withValues(alpha: .7))),
        child: Column(crossAxisAlignment: CrossAxisAlignment.stretch, children: [
          Row(crossAxisAlignment: CrossAxisAlignment.end, children: [
            Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
              const Text('LIVE DIRECTORY', style: TextStyle(fontSize: 11, fontWeight: FontWeight.w800, letterSpacing: .4, color: HadirBrand.primary)),
              const SizedBox(height: 4),
              Text.rich(TextSpan(children: [const TextSpan(text: 'قائمة الموظفين ', style: TextStyle(fontSize: 18, fontWeight: FontWeight.w900)), TextSpan(text: '(${filtered.length})', style: TextStyle(fontSize: 17, color: Theme.of(context).colorScheme.onSurfaceVariant))])),
              const SizedBox(height: 4),
              Text('المالك والمدير: إدارة الموظفين والتحضير المباشر للمهمات والمأموريات وتسجيل الهروب والعودة والأذونات والإجازات.', style: TextStyle(fontSize: 10.5, height: 1.5, color: Theme.of(context).colorScheme.onSurfaceVariant)),
            ])),
            const SizedBox(width: 10),
            Text('${filtered.length} نتيجة', style: TextStyle(fontSize: 10, fontWeight: FontWeight.w800, color: Theme.of(context).colorScheme.onSurfaceVariant)),
          ]),
          const SizedBox(height: 14),
          LayoutBuilder(builder: (context, box) {
            final status = DropdownButtonFormField<String>(initialValue: _employeeStatus, isDense: true, decoration: const InputDecoration(labelText: 'الحالة'), items: const [DropdownMenuItem(value: 'all', child: Text('كل الحالات')), DropdownMenuItem(value: 'active', child: Text('فعال')), DropdownMenuItem(value: 'suspended', child: Text('موقوف'))], onChanged: (v) => setState(() => _employeeStatus = v ?? 'all'));
            final schedule = DropdownButtonFormField<String>(initialValue: _employeeSchedule, isDense: true, decoration: const InputDecoration(labelText: 'نوع الدوام'), items: const [DropdownMenuItem(value: 'all', child: Text('كل أنواع الدوام')), DropdownMenuItem(value: 'ADMIN', child: Text('إداري')), DropdownMenuItem(value: 'ROTATION', child: Text('تناوبي'))], onChanged: (v) => setState(() => _employeeSchedule = v ?? 'all'));
            if (box.maxWidth >= 720) return Row(children: [Expanded(child: _searchBox(hint: 'بحث بالاسم أو الرقم أو الجهاز', onChanged: (v) => setState(() => _employeeQuery = v))), const SizedBox(width: 8), SizedBox(width: 145, child: status), const SizedBox(width: 8), SizedBox(width: 155, child: schedule)]);
            return Column(children: [_searchBox(hint: 'بحث بالاسم أو الرقم أو الجهاز', onChanged: (v) => setState(() => _employeeQuery = v)), const SizedBox(height: 8), Row(children: [Expanded(child: status), const SizedBox(width: 8), Expanded(child: schedule)])]);
          }),
          const SizedBox(height: 16),
          if (_loading) const Padding(padding: EdgeInsets.symmetric(vertical: 50), child: Center(child: Text('جاري مزامنة الموظفين من قاعدة بيانات النظام…', style: TextStyle(fontSize: 12))))
          else if (filtered.isEmpty) Container(padding: const EdgeInsets.symmetric(vertical: 48), decoration: BoxDecoration(border: Border.all(color: Theme.of(context).colorScheme.outlineVariant.withValues(alpha: .7)), borderRadius: BorderRadius.circular(16)), child: Column(children: [const Text('⌕', style: TextStyle(fontSize: 30)), const SizedBox(height: 8), const Text('لا توجد نتائج', style: TextStyle(fontSize: 13, fontWeight: FontWeight.w900)), const SizedBox(height: 4), Text('جرّب تغيير البحث أو الفلاتر.', style: TextStyle(fontSize: 10, color: Theme.of(context).colorScheme.onSurfaceVariant))]))
          else LayoutBuilder(builder: (context, constraints) {
            final columns = constraints.maxWidth >= 1100 ? 3 : constraints.maxWidth >= 720 ? 2 : 1;
            final gap = 12.0;
            final cardWidth = (constraints.maxWidth - (gap * (columns - 1))) / columns;
            return Wrap(spacing: gap, runSpacing: gap, children: filtered.map((raw) {
              final employee = _employeeWithControls(Map<String, dynamic>.from(raw as Map));
              final id = '${employee['id'] ?? ''}';
              final name = '${employee['name'] ?? 'بدون اسم'}'.trim();
              final status = '${employee['status'] ?? 'active'}';
              final isVip = employee['isVip'] == true;
              final autoIn = employee['autoCheckIn'] == true;
              final autoOut = employee['autoCheckOut'] == true;
              final escapeStatus = '${employee['escapeStatus'] ?? employee['fieldStatus'] ?? 'none'}'.toLowerCase();
              final isEscaped = escapeStatus == 'escaped' || escapeStatus == 'escape';
              final hasDevice = '${employee['deviceId'] ?? ''}'.trim().isNotEmpty;
              final stateColor = _statusColor(status, escaped: isEscaped, hasDevice: hasDevice);
              final stateLabel = isEscaped ? 'هارب' : status.toLowerCase() == 'active' ? (hasDevice ? 'فعال ومرتبط' : 'فعال وغير مرتبط') : 'موقوف';
              final location = _locations.where((l) => '${l['id'] ?? ''}' == '${employee['locationId'] ?? ''}').firstOrNull;
              return SizedBox(width: cardWidth, child: Container(padding: const EdgeInsets.all(12), decoration: BoxDecoration(color: Theme.of(context).colorScheme.surface.withValues(alpha: .76), borderRadius: BorderRadius.circular(18), border: Border.all(color: Theme.of(context).colorScheme.outlineVariant.withValues(alpha: .65))), child: Column(crossAxisAlignment: CrossAxisAlignment.stretch, children: [
                Row(children: [Container(width: 42, height: 42, decoration: BoxDecoration(color: stateColor.withValues(alpha: .09), borderRadius: BorderRadius.circular(13), border: Border.all(color: Theme.of(context).colorScheme.primary.withValues(alpha: .20))), child: Center(child: Text(name.isEmpty ? 'م' : name.substring(0, 1), style: TextStyle(color: stateColor, fontWeight: FontWeight.w900)))), const SizedBox(width: 10), Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [Text(name, maxLines: 1, overflow: TextOverflow.ellipsis, style: const TextStyle(fontSize: 13, fontWeight: FontWeight.w900)), const SizedBox(height: 2), Text('${employee['jobNumber'] ?? '—'}', style: TextStyle(fontSize: 10, color: Theme.of(context).colorScheme.onSurfaceVariant, fontFamily: 'monospace'))])), Column(crossAxisAlignment: CrossAxisAlignment.end, children: [Row(mainAxisSize: MainAxisSize.min, children: [Container(width: 7, height: 7, decoration: BoxDecoration(color: stateColor, shape: BoxShape.circle)), const SizedBox(width: 5), Container(padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4), decoration: BoxDecoration(color: stateColor.withValues(alpha: .09), borderRadius: BorderRadius.circular(20)), child: Text(stateLabel, style: TextStyle(color: stateColor, fontSize: 9, fontWeight: FontWeight.w800)))]), if (isVip) const Padding(padding: EdgeInsets.only(top: 4), child: Text('★ VIP', style: TextStyle(color: Color(0xFFF59E0B), fontSize: 10, fontWeight: FontWeight.w900)))]),]),
                const SizedBox(height: 10),
                Wrap(spacing: 6, runSpacing: 6, children: [_infoPill('الدوام', employee['scheduleType'] == 'ROTATION' ? 'تناوبي' : 'إداري'), _infoPill('الموقع', '${location?['name'] ?? 'المقر الرئيسي'}'), _infoPill('الوقت', employee['scheduleType'] == 'ROTATION' ? '${employee['rotationDaysOn'] ?? 7} عمل / ${employee['rotationDaysOff'] ?? 7} راحة' : '${employee['workStartTime'] ?? '--:--'} → ${employee['workEndTime'] ?? '--:--'}'), _infoPill('الميدان', isEscaped ? 'هارب من العمل' : escapeStatus == 'returned' ? 'عاد للعمل' : 'طبيعي'), _infoPill('التأخير', '${employee['gracePeriodMinutes'] ?? 0} دقيقة'), if (hasDevice) _infoPill('الجهاز', '${employee['deviceLabel'] ?? 'مرتبط'}'), if (autoIn || autoOut) _infoPill('الأتمتة', '${autoIn ? 'حضور' : ''}${autoIn && autoOut ? ' + ' : ''}${autoOut ? 'انصراف' : ''}')]),
                const SizedBox(height: 10),
                Wrap(spacing: 6, runSpacing: 6, children: [
                  _smallAction('إذن', Icons.event_available_rounded, () => _employeeRequest(id, name, 'permission'), color: const Color(0xFF0284C7)),
                  _smallAction('إجازة', Icons.beach_access_rounded, () => _employeeRequest(id, name, 'leave'), color: const Color(0xFF7C3AED)),
                  _smallAction('تحضير', Icons.login_rounded, () => _directAttendance(id, name, 'check-in'), color: const Color(0xFF16A34A)),
                  _smallAction('انصراف', Icons.logout_rounded, () => _directAttendance(id, name, 'check-out'), color: const Color(0xFFF97316)),
                  _smallAction(isEscaped ? 'عودة' : 'هروب', isEscaped ? Icons.rotate_left_rounded : Icons.directions_walk_rounded, () => _changeEscape(id, name, isEscaped ? 'returned' : 'escaped'), color: isEscaped ? HadirBrand.primary : const Color(0xFFDC2626)),
                  _smallAction('تعديل', Icons.edit_rounded, () => _editEmployee(employee)),
                  _smallAction('قوى', Icons.auto_awesome_rounded, () => _showWorkforceControls(id, name), color: const Color(0xFFF59E0B)),
                  _smallAction('جهاز', Icons.smartphone_rounded, () => _request('DELETE', '/api/employees/$id/device')),
                  _smallAction('حذف', Icons.delete_outline_rounded, () => _deleteEmployee(id, name), color: const Color(0xFFDC2626)),
                ]),
              ]));
            }).toList());
          }),
        ]),
      ),
    ]);
  }

  Widget _requestsView() {
    final filtered = _requests.where((raw) { if (raw is! Map) return false; final request = Map<String, dynamic>.from(raw); final query = _requestQuery.trim().toLowerCase(); final status = '${request['status'] ?? 'pending'}'.toLowerCase(); final text = '${request['type'] ?? ''} ${request['employeeName'] ?? ''} ${request['employeeId'] ?? ''} ${request['reason'] ?? ''}'.toLowerCase(); return (query.isEmpty || text.contains(query)) && (_requestStatus == 'all' || status == _requestStatus); }).toList();
    final pending = _requests.where((r) => r is Map && '${r['status'] ?? 'pending'}' == 'pending').length;
    return Column(crossAxisAlignment: CrossAxisAlignment.stretch, children: [Row(children: [_metric('إجمالي الطلبات', '${_requests.length}', Icons.inbox_rounded), const SizedBox(width: 8), _metric('قيد المراجعة', '$pending', Icons.pending_actions_rounded)]), const SizedBox(height: 8), _searchBox(hint: 'ابحث في نوع الطلب أو الموظف أو السبب', onChanged: (v) => setState(() => _requestQuery = v)), const SizedBox(height: 8), Wrap(spacing: 8, children: [_filterChip('الكل', 'all', _requestStatus, (v) => setState(() => _requestStatus = v)), _filterChip('معلق', 'pending', _requestStatus, (v) => setState(() => _requestStatus = v)), _filterChip('مقبول', 'approved', _requestStatus, (v) => setState(() => _requestStatus = v)), _filterChip('مرفوض', 'rejected', _requestStatus, (v) => setState(() => _requestStatus = v))]), const SizedBox(height: 12), if (filtered.isEmpty) _empty('لا توجد طلبات مطابقة.') else ...filtered.map((raw) { final request = Map<String, dynamic>.from(raw as Map); final id = '${request['id'] ?? ''}'; final status = '${request['status'] ?? 'pending'}'; final color = _statusColor(status); return _card(Container(decoration: BoxDecoration(border: Border(right: BorderSide(color: color, width: 4)), borderRadius: BorderRadius.circular(12)), child: ListTile(title: Text('${request['type'] ?? 'طلب'} · ${request['employeeName'] ?? request['employeeId'] ?? ''}', style: const TextStyle(fontWeight: FontWeight.w800)), subtitle: Wrap(spacing: 8, children: [Text('${request['reason'] ?? ''}'), Text('الحالة: $status', style: TextStyle(color: color, fontWeight: FontWeight.w800))]), isThreeLine: true, trailing: status == 'pending' ? PopupMenuButton<String>(onSelected: (value) => _request('PATCH', '/api/requests/$id', data: {'status': value}), itemBuilder: (_) => const [PopupMenuItem(value: 'approved', child: Text('موافقة')), PopupMenuItem(value: 'rejected', child: Text('رفض'))]) : null))); })]);
  }

  Widget _reportsView() { final successful = _audit.where((raw) => raw is Map && raw['result'] == 'success' && (raw['action'] == 'check-in' || raw['action'] == 'check-out')).toList(); final checkIns = successful.where((r) => (r as Map)['action'] == 'check-in').length, checkOuts = successful.where((r) => (r as Map)['action'] == 'check-out').length; return Column(crossAxisAlignment: CrossAxisAlignment.stretch, children: [Row(children: [_metric('سجلات ناجحة', '${successful.length}', Icons.fact_check_rounded), const SizedBox(width: 8), _metric('حضور', '$checkIns', Icons.login_rounded), const SizedBox(width: 8), _metric('انصراف', '$checkOuts', Icons.logout_rounded)]), const SizedBox(height: 12), _card(const ListTile(title: Text('التقرير التفصيلي', style: TextStyle(fontWeight: FontWeight.w900)), subtitle: Text('عمليات الحضور والانصراف المستخرجة من سجل التدقيق.'))), ...successful.take(80).map((raw) { final item = Map<String, dynamic>.from(raw as Map); final color = _statusColor(item['action'] == 'check-in' ? 'active' : 'approved'); return _card(Container(decoration: BoxDecoration(border: Border(right: BorderSide(color: color, width: 3)), borderRadius: BorderRadius.circular(12)), child: ListTile(title: Text('${item['actorName'] ?? item['jobNumber'] ?? 'موظف'} · ${item['action']}', style: const TextStyle(fontWeight: FontWeight.w800)), subtitle: Text('${item['timestamp'] ?? ''} · ${item['distanceMeters'] ?? 0} متر')))); })]); }

  Widget _auditView() { if (_audit.isEmpty) return _empty('لا توجد أحداث تدقيق.'); return Column(crossAxisAlignment: CrossAxisAlignment.stretch, children: [Row(children: [_metric('إجمالي الأحداث', '${_audit.length}', Icons.security_rounded), const SizedBox(width: 8), _metric('ناجح', '${_audit.where((r) => r is Map && r['result'] == 'success').length}', Icons.verified_rounded)]), const SizedBox(height: 12), ..._audit.take(120).map((raw) { final item = Map<String, dynamic>.from(raw as Map); final color = _statusColor('${item['result'] ?? ''}'); return _card(Container(decoration: BoxDecoration(border: Border(right: BorderSide(color: color, width: 3)), borderRadius: BorderRadius.circular(12)), child: ListTile(dense: true, title: Text('${item['action'] ?? 'حدث'} · ${item['actorName'] ?? item['jobNumber'] ?? ''}', style: const TextStyle(fontWeight: FontWeight.w800)), subtitle: Text('${item['timestamp'] ?? ''} · ${item['result'] ?? ''}', style: TextStyle(color: color))))); })]); }

  Widget _adminsView() { final filtered = _admins.where((raw) { if (raw is! Map) return false; final admin = Map<String, dynamic>.from(raw); final query = _adminQuery.trim().toLowerCase(); return query.isEmpty || '${admin['name'] ?? ''} ${admin['username'] ?? ''} ${admin['role'] ?? ''}'.toLowerCase().contains(query); }).toList(); return Column(crossAxisAlignment: CrossAxisAlignment.stretch, children: [Row(children: [_metric('الحسابات', '${_admins.length}', Icons.admin_panel_settings_rounded), const Spacer(), FilledButton.icon(onPressed: _addAdmin, icon: const Icon(Icons.person_add), label: const Text('إضافة'))]), const SizedBox(height: 8), _searchBox(hint: 'ابحث بالاسم أو اسم المستخدم أو الصلاحية', onChanged: (v) => setState(() => _adminQuery = v)), const SizedBox(height: 12), if (filtered.isEmpty) _empty('لا توجد حسابات مطابقة.') else ...filtered.map((raw) { final admin = Map<String, dynamic>.from(raw as Map); final id = '${admin['id'] ?? ''}'; final color = _statusColor(admin['active'] == false ? 'inactive' : 'active'); return _card(Container(decoration: BoxDecoration(border: Border(right: BorderSide(color: color, width: 3)), borderRadius: BorderRadius.circular(12)), child: ListTile(leading: Icon(Icons.admin_panel_settings_rounded, color: color), title: Text('${admin['name'] ?? ''}', style: const TextStyle(fontWeight: FontWeight.w800)), subtitle: Text('@${admin['username'] ?? ''} · ${admin['role'] ?? ''}'), trailing: Switch(value: admin['active'] != false, onChanged: (v) => _request('PATCH', '/api/admins/$id', data: {'active': v}))))); })]); }

  Widget _settingsView() { if (_settings.isEmpty) return _empty('لا توجد إعدادات متاحة.'); final rows = _settings.entries.map((e) => Padding(padding: const EdgeInsets.symmetric(vertical: 7), child: Row(children: [Expanded(child: Text(e.key, style: const TextStyle(fontWeight: FontWeight.w700))), Flexible(child: Text('${e.value}', textAlign: TextAlign.left, style: TextStyle(color: Theme.of(context).colorScheme.onSurfaceVariant)))]))).toList(); return Column(crossAxisAlignment: CrossAxisAlignment.stretch, children: [const Text('إعدادات النظام', style: TextStyle(fontSize: 20, fontWeight: FontWeight.w900)), const SizedBox(height: 8), _card(Padding(padding: const EdgeInsets.all(16), child: Column(children: rows))) ]); }
  Widget _empty(String message) => Card(child: Padding(padding: const EdgeInsets.all(28), child: Column(children: [const Icon(Icons.inbox_outlined, size: 42), const SizedBox(height: 10), Text(message, textAlign: TextAlign.center)])));

  @override
  Widget build(BuildContext context) {
    final views = [_employeesView(), _requestsView(), _reportsView(), _auditView(), _adminsView(), _settingsView()];
    const labels = ['الموظفون', 'الطلبات', 'التقارير', 'التدقيق', 'الإدارة', 'الإعدادات'];
    const icons = [Icons.groups_rounded, Icons.event_note_rounded, Icons.bar_chart_rounded, Icons.security_rounded, Icons.admin_panel_settings_rounded, Icons.settings_rounded];
    return Scaffold(appBar: AppBar(title: const Text('إدارة حاضر', style: TextStyle(fontWeight: FontWeight.w900)), leading: IconButton(onPressed: () => context.go('/admin'), icon: const Icon(Icons.arrow_back))), body: _loading ? const Center(child: CircularProgressIndicator()) : _error != null ? Center(child: Padding(padding: const EdgeInsets.all(24), child: Column(mainAxisSize: MainAxisSize.min, children: [const Icon(Icons.cloud_off_rounded, size: 48), const SizedBox(height: 12), Text(_error!, textAlign: TextAlign.center), const SizedBox(height: 12), FilledButton(onPressed: _load, child: const Text('إعادة المحاولة'))]))) : RefreshIndicator(onRefresh: _load, child: ListView(padding: const EdgeInsets.fromLTRB(16, 12, 16, 32), children: [SingleChildScrollView(scrollDirection: Axis.horizontal, child: Row(children: List.generate(labels.length, (index) => Padding(padding: const EdgeInsets.only(left: 8), child: ChoiceChip(selected: _tab == index, avatar: Icon(icons[index], size: 17), label: Text(labels[index]), onSelected: (_) => setState(() => _tab = index)))))), const SizedBox(height: 18), views[_tab]])));
  }
}
