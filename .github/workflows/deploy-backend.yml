name: Deploy Hadir API to Cloudflare Workers

on:
  push:
    branches: [main]
    paths:
      - "backend/**"
      - ".github/workflows/deploy-backend.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.15
      - name: Install Wrangler
        run: bun add --dev wrangler@latest
      - name: Deploy Worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          JWT_SECRET: ${{ secrets.HADIR_JWT_SECRET }}
          OWNER_USERNAME: ${{ secrets.HADIR_OWNER_USERNAME }}
          OWNER_PASSWORD: ${{ secrets.HADIR_OWNER_PASSWORD }}
        run: |
          bunx wrangler deploy --var JWT_SECRET:"$JWT_SECRET" --var OWNER_USERNAME:"$OWNER_USERNAME" --var OWNER_PASSWORD:"$OWNER_PASSWORD"
