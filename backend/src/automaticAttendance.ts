import { getAttendanceShift } from "./attendance-period";

type Env={DB:D1Database; APP_TIMEZONE?:string};
type Admin={id:string;name?:string;role:string};
const now=()=>new Date().toISOString();
const id=()=>crypto.randomUUID();
const cors=(origin:string)=>({"content-type":"application/json; charset=utf-8","access-control-allow-origin":origin,"access-control-allow-credentials":"true","cache-control":"no-store"});
const json=(data:unknown,status=200,origin="*")=>new Response(JSON.stringify(data),{status,headers:cors(origin)});
const OWNER="المالك فقط يستطيع تنفيذ التحضير أو الانصراف المباشر أو تعديل التلقائي";
function roleIsOwner(actor:Admin|null){return !!actor&&String(actor.role).toLowerCase()==="owner";}
function dateKey(date:Date,tz:string){return new Intl.DateTimeFormat("en-CA",{timeZone:tz,year:"numeric",month:"2-digit",day:"2-digit"}).format(date);}
async function locationFor(db:D1Database,employee:any){const row=await db.prepare("SELECT id,lat,lng,radius_meters AS radiusMeters FROM locations WHERE id=? LIMIT 1").bind(employee.locationId||"main").first<any>();return row||(await db.prepare("SELECT id,lat,lng,radius_meters AS radiusMeters FROM locations ORDER BY name LIMIT 1").first<any>());}
async function insertAttendance(db:D1Database,employee:any,type:"check-in"|"check-out",timestamp:string,actorName:string,reason:string){const loc=await locationFor(db,employee);if(!loc)return null;const deviceId=`ADMIN_DIRECT:${actorName}`;const aid=id();await db.prepare("INSERT INTO attendance(id,employee_id,job_number,employee_name,type,timestamp,lat,lng,distance_meters,device_id,ip,qr_code,location_id) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)").bind(aid,employee.id,employee.jobNumber,employee.name,type,timestamp,Number(loc.lat),Number(loc.lng),0,deviceId,"system","AUTO_DIRECT",loc.id).run();await db.prepare("INSERT INTO audit(id,employee_id,job_number,actor_name,action,result,reason,timestamp,device_id,ip,lat,lng,distance_meters) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)").bind(id(),employee.id,employee.jobNumber,actorName,type,"success",reason,timestamp,deviceId,"system",Number(loc.lat),Number(loc.lng),0).run().catch(()=>undefined);return{id:aid,employeeId:employee.id,employeeName:employee.name,type,timestamp,locationId:loc.id};}
export async function directAttendance(req:Request,env:Env,actor:Admin|null,origin:string){const path=new URL(req.url).pathname;if(!["/api/manager/attendance/checkout","/api/manager/attendance/check-in","/api/manager/attendance"].includes(path)||req.method!=="POST")return null;if(!actor||!["owner","manager"].includes(String(actor.role).toLowerCase()))return json({error:OWNER},403,origin);const b=await req.json().catch(()=>({})) as any;const employeeId=String(b.employeeId||"").trim();if(!employeeId)return json({error:"الموظف مطلوب"},400,origin);const employee=await env.DB.prepare("SELECT id,job_number AS jobNumber,name,status,location_id AS locationId,schedule_type AS scheduleType,rotation_start_date AS rotationStartDate,rotation_days_on AS rotationDaysOn,rotation_days_off AS rotationDaysOff,work_start_time AS workStartTime,work_end_time AS workEndTime,work_days_json AS workDaysJson FROM employees WHERE id=? LIMIT 1").bind(employeeId).first<any>();if(!employee||employee.status!=="active")return json({error:"الموظف غير موجود أو موقوف"},404,origin);const type=path.endsWith("checkout")?"check-out":path.endsWith("check-in")?"check-in":b.type==="check-out"?"check-out":"check-in";const current=new Date();const shift=getAttendanceShift(employee,current);if(!shift.isWorkDay)return json({error:"لا يوجد دوام للموظف الآن"},403,origin);const rows=await env.DB.prepare("SELECT type,timestamp FROM attendance WHERE employee_id=? AND timestamp>=? AND timestamp<=? ORDER BY timestamp ASC").bind(employeeId,shift.start.toISOString(),new Date(Math.min(shift.end.getTime()+60000,current.getTime())).toISOString()).all<any>();const periodRows=(rows.results||[]) as any[];const last=periodRows[periodRows.length-1];if(type==="check-in"&&periodRows.some((r:any)=>r.type==="check-in"))return json({error:"الموظف مسجل حضور بالفعل لهذه المناوبة"},409,origin);if(type==="check-out"&&last?.type!=="check-in")return json({error:"لا يمكن تسجيل الانصراف قبل تسجيل الحضور لهذه المناوبة"},409,origin);if(type==="check-out"&&current.getTime()<shift.end.getTime())return json({error:"لم ينتهِ وقت دوام الموظف بعد"},403,origin);const record=await insertAttendance(env.DB,employee,type,current.toISOString(),actor?.name||(String(actor.role).toLowerCase()==="manager"?"المدير":"المالك"),type==="check-in"?"تحضير مباشر لمهمة/مأمورية":"انصراف مباشر لمهمة/مأمورية");if(!record)return json({error:"لا يوجد موقع عمل محفوظ"},409,origin);return json({ok:true,record},201,origin);}
export async function workforceControls(req:Request,env:Env,actor:Admin|null,origin:string){const u=new URL(req.url);if(!u.pathname.startsWith("/api/manager/workforce-controls"))return null;if(!actor||!["owner","manager","supervisor"].includes(String(actor.role).toLowerCase()))return json({error:"غير مصرح"},403,origin);if(req.method==="GET"){const rows=await env.DB.prepare("SELECT id,job_number AS jobNumber,name,status,schedule_type AS scheduleType,rotation_start_date AS rotationStartDate,rotation_days_on AS rotationDaysOn,rotation_days_off AS rotationDaysOff,work_start_time AS workStartTime,work_end_time AS workEndTime,work_days_json AS workDaysJson,is_vip AS isVip,auto_check_in AS autoCheckIn,auto_check_out AS autoCheckOut FROM employees ORDER BY name").all<any>();return json((rows.results||[]).map((e:any)=>({...e,isVip:Boolean(e.isVip),autoCheckIn:Boolean(e.autoCheckIn),autoCheckOut:Boolean(e.autoCheckOut),workDays:(()=>{try{return JSON.parse(e.workDaysJson||"[]")}catch{return[]}})()})),200,origin);}const m=u.pathname.match(/^\/api\/manager\/workforce-controls\/([^/]+)$/);if(!m||req.method!=="PATCH")return json({error:"WORKFORCE_CONTROL_ROUTE_NOT_FOUND"},404,origin);if(!roleIsOwner(actor))return json({error:OWNER},403,origin);const employeeId=decodeURIComponent(m[1]);const e=await env.DB.prepare("SELECT id FROM employees WHERE id=? LIMIT 1").bind(employeeId).first();if(!e)return json({error:"EMPLOYEE_NOT_FOUND"},404,origin);const b=await req.json().catch(()=>({})) as any;const fields:string[]=[];const values:any[]=[];const vip=typeof b.isVip==="boolean"?b.isVip:null;const hasAutoCheckIn=typeof b.autoCheckIn==="boolean";const hasAutoCheckOut=typeof b.autoCheckOut==="boolean";if(vip!==null){fields.push("is_vip=?","auto_check_in=?","auto_check_out=?");values.push(vip?1:0,vip?1:0,vip?1:0);}else{if(hasAutoCheckIn){fields.push("auto_check_in=?");values.push(b.autoCheckIn?1:0);}if(hasAutoCheckOut){fields.push("auto_check_out=?");values.push(b.autoCheckOut?1:0);}}if(!fields.length)return json({error:"لا توجد تغييرات"},400,origin);values.push(employeeId);await env.DB.prepare(`UPDATE employees SET ${fields.join(",")} WHERE id=?`).bind(...values).run();return json({ok:true,employeeId},200,origin);}
export async function runAutomaticAttendance(env:Env){const configured=await env.DB.prepare("SELECT value FROM settings WHERE key='timezone' LIMIT 1").first<any>().catch(()=>null);let tz=String(env.APP_TIMEZONE||"Asia/Damascus");try{const parsed=JSON.parse(String(configured?.value||""));if(typeof parsed==="string"&&parsed.trim())tz=parsed.trim();}catch{if(String(configured?.value||"").trim())tz=String(configured.value).trim();}const current=new Date();const currentDay=dateKey(current,tz);const employees=await env.DB.prepare("SELECT id,job_number AS jobNumber,name,status,location_id AS locationId,schedule_type AS scheduleType,rotation_start_date AS rotationStartDate,rotation_days_on AS rotationDaysOn,rotation_days_off AS rotationDaysOff,work_start_time AS workStartTime,work_end_time AS workEndTime,work_days_json AS workDaysJson,is_vip AS isVip,auto_check_in AS autoCheckIn,auto_check_out AS autoCheckOut FROM employees WHERE status='active' AND (is_vip=1 OR auto_check_in=1 OR auto_check_out=1)").all<any>();const results:any[]=[];for(const e of (employees.results||[]) as any[]){const activeRequest=await env.DB.prepare("SELECT type,start_date AS startDate,end_date AS endDate FROM requests WHERE employee_id=? AND status IN ('approved','confirmed') AND type IN ('leave','permission') AND COALESCE(start_date,substr(created_at,1,10))<=? AND COALESCE(end_date,COALESCE(start_date,substr(created_at,1,10)))>=? ORDER BY created_at DESC LIMIT 1").bind(e.id,currentDay,currentDay).first<any>().catch(()=>null);if(activeRequest)continue;const shift=getAttendanceShift(e,current,tz);if(!shift.isWorkDay)continue;const existing=await env.DB.prepare("SELECT type,timestamp FROM attendance WHERE employee_id=? AND timestamp>=? AND timestamp<? ORDER BY timestamp ASC").bind(e.id,shift.start.toISOString(),new Date(shift.end.getTime()+60000).toISOString()).all<any>();const shiftRows=(existing.results||[]) as any[];let hasShiftIn=shiftRows.some(r=>r.type==="check-in");const hasShiftOut=shiftRows.some(r=>r.type==="check-out");if((e.autoCheckIn||e.isVip)&&current>=shift.start&&!hasShiftIn){const r=await insertAttendance(env.DB,e,"check-in",shift.start.toISOString(),"التلقائي","تحضير تلقائي حسب بداية المناوبة للموظف VIP/التلقائي");if(r){shiftRows.push({type:r.type,timestamp:r.timestamp});hasShiftIn=true;results.push(r);}}const latestShiftIn=[...shiftRows].filter(r=>r.type==="check-in"&&Date.parse(String(r.timestamp))<=shift.end.getTime()).sort((a,b)=>Date.parse(String(b.timestamp))-Date.parse(String(a.timestamp)))[0];if((e.autoCheckOut||e.isVip)&&!hasShiftOut&&latestShiftIn&&current>=shift.end){const r=await insertAttendance(env.DB,e,"check-out",shift.end.toISOString(),"التلقائي","انصراف تلقائي حسب نهاية المناوبة للموظف VIP/التلقائي");if(r)results.push(r);}}return{ok:true,timezone:tz,processed:employees.results?.length||0,created:results};}