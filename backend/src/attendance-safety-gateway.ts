import production, { HadirRealtime } from "./attendance-production-gateway";
import { getAttendanceShift } from "./attendance-period";
export { HadirRealtime };
type Env={DB:D1Database;REALTIME:DurableObjectNamespace;APP_ORIGIN?:string;APP_ORIGINS?:string;APP_TIMEZONE?:string;JWT_SECRET?:string;OWNER_RECOVERY_CODE?:string;PROFILE_IMAGES?:R2Bucket;REPORT_ARCHIVE?:R2Bucket;BROWSER?:BrowserRun};
async function currentUser(request:Request,env:Env,ctx:ExecutionContext){const u=new URL(request.url);u.pathname="/api/me";u.search="";const r=await production.fetch(new Request(u,{method:"GET",headers:request.headers}),env,ctx);if(!r.ok)return null;const d=await r.json().catch(()=>({})) as any;return d?.user||null;}
async function isStaff(request:Request,env:Env,ctx:ExecutionContext){const user=await currentUser(request,env,ctx);return user?.role==="staff";}
function origin(request:Request,env:Env){const incoming=String(request.headers.get("origin")||"").trim().replace(/\/$/,"");const configured=String(env.APP_ORIGIN||env.APP_ORIGINS||"").split(",").map(v=>v.trim().replace(/\/$/,"")).filter(Boolean);return incoming&&configured.includes(incoming)?incoming:configured[0]||"*";}
async function prepareEmployee(request:Request,env:Env,ctx:ExecutionContext){const actor=await currentUser(request,env,ctx),role=String(actor?.role||"").toLowerCase();if(!actor||!["owner","manager"].includes(role))return new Response(JSON.stringify({error:"المالك أو المدير فقط يستطيعان التحضير المباشر"}),{status:403,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});const body=await request.json().catch(()=>({})) as any,employeeId=String(body.employeeId||"").trim(),type=String(body.type||"check-in");if(!employeeId||type!=="check-in")return new Response(JSON.stringify({error:"بيانات التحضير المباشر غير صحيحة"}),{status:400,headers:{"content-type":"application/json; charset=utf-8"}});const employee=await env.DB.prepare("SELECT id,job_number AS jobNumber,name,status,schedule_type AS scheduleType,rotation_start_date AS rotationStartDate,rotation_days_on AS rotationDaysOn,rotation_days_off AS rotationDaysOff,work_start_time AS workStartTime,work_end_time AS workEndTime,work_days_json AS workDaysJson FROM employees WHERE id=? LIMIT 1").bind(employeeId).first<any>();if(!employee||employee.status!=="active")return new Response(JSON.stringify({error:"الموظف غير موجود أو موقوف"}),{status:404,headers:{"content-type":"application/json; charset=utf-8"}});const now=new Date(),shift=getAttendanceShift(employee,now,env.APP_TIMEZONE||"Asia/Damascus");if(!shift.isWorkDay||now.getTime()<shift.start.getTime()||now.getTime()>shift.end.getTime()+60000)return new Response(JSON.stringify({error:"لا توجد مناوبة فعالة لهذا الموظف الآن"}),{status:409,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});const rows=await env.DB.prepare("SELECT id,type,timestamp FROM attendance WHERE employee_id=? AND timestamp>=? AND timestamp<=? ORDER BY timestamp DESC LIMIT 200").bind(employee.id,shift.start.toISOString(),new Date(Math.min(shift.end.getTime()+60000,now.getTime()+5000)).toISOString()).all<any>();const valid=(rows.results||[]).filter((r:any)=>{const t=Date.parse(String(r.timestamp||""));return Number.isFinite(t)&&t>=shift.start.getTime()&&t<=now.getTime()+5000;});if(valid.some((r:any)=>String(r.type)==="check-in"))return new Response(JSON.stringify({error:"الموظف مسجل حضور بالفعل في هذه المناوبة"}),{status:409,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});const id=crypto.randomUUID(),timestamp=now.toISOString(),deviceId=`ADMIN_DIRECT:${role==="manager"?"المدير":"المالك"}`;await env.DB.prepare("INSERT INTO attendance(id,employee_id,job_number,employee_name,type,timestamp,lat,lng,distance_meters,device_id,ip,qr_code,location_id) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)").bind(id,employee.id,employee.jobNumber,employee.name,"check-in",timestamp,null,null,null,deviceId,"system","DIRECT_ADMIN",null).run();await env.DB.prepare("INSERT INTO audit(id,employee_id,job_number,actor_name,action,result,reason,timestamp,device_id,ip,lat,lng,distance_meters) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)").bind(crypto.randomUUID(),employee.id,employee.jobNumber,actor.name||(role==="manager"?"المدير":"المالك"),"check-in","success","تحضير مباشر من الإدارة",timestamp,deviceId,"system",null,null,null).run().catch(()=>undefined);return new Response(JSON.stringify({ok:true,record:{id,employeeId:employee.id,jobNumber:employee.jobNumber,employeeName:employee.name,type:"check-in",timestamp,deviceId,ip:"system",qrCode:"DIRECT_ADMIN",locationId:null}}),{status:201,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});}
function sanitizeAttendanceResponse(response:Response){return response.clone().json().then((data:any)=>{if(!Array.isArray(data))return response;const now=Date.now(),safe=data.filter((row:any)=>{const t=Date.parse(String(row?.timestamp||""));return Number.isFinite(t)&&t<=now+5000;});return new Response(JSON.stringify(safe),{status:response.status,headers:response.headers});}).catch(()=>response);}
export default {async fetch(request:Request,env:Env,ctx:ExecutionContext){const url=new URL(request.url),attendanceGet=(url.pathname==="/api/attendance"||url.pathname==="/api/attendance/")&&request.method==="GET",managerPrepare=(url.pathname==="/api/manager/attendance"||url.pathname==="/api/manager/attendance/")&&request.method==="POST";if(managerPrepare)return prepareEmployee(request,env,ctx);const response=await production.fetch(request,env,ctx);if(!attendanceGet||!response.ok)return response;if(!(await isStaff(request,env,ctx)))return response;return sanitizeAttendanceResponse(response);},async scheduled(controller:ScheduledEvent,env:Env,ctx:ExecutionContext){const scheduled=(production as any).scheduled;if(typeof scheduled==="function")return scheduled(controller,env,ctx);}};
