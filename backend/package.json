{
  "name": "hadir-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply Hadir --remote",
    "prepare": "node scripts/patch-employee-save.mjs"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.2.2",
    "@mmmike/web-push": "^1.3.0"
  },
  "devDependencies": {
    "wrangler": "4.123.0"
  }
}
